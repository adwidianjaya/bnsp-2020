import { sum, diff, absoluteDiff } from "./numbers";

let sumResult: number = sum(10, 7);
console.log({ sumResult });

let diffResult: number = diff(7, 10);
console.log({ diffResult });

let absoluteDiffResult: number = absoluteDiff(7, 10);
console.log({ absoluteDiffResult });
